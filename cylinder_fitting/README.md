# 实现圆柱体拟合
> 目前本算法只是实现单个圆柱体拟合，也就是说如果点云包含多个圆柱体是不支持的。而且本算法对噪声敏感，本身没有处理噪声，需要先进行降噪处理。

## 目录结构

- src
  - cmath.h: 主要扩展弧度转角度，角度转弧度的运算 

# 2021-03-15

1. 修正创建接口时，跳出作用域删除算法对象的问题；
2. 修正拟合圆柱体函数卡死问题。因为拟合阈值太小，导致拟合过慢。

